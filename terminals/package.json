{
  "name": "pixel-terminals",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/terminals/ghostty.jxa dist/terminals/",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test \"test/*.test.js\""
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
